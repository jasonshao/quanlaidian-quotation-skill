interface:
  display_name: "全来店报价单"
  short_description: "OpenClaw/飞书均可用：优先表单，未弹出时走引导式选择对话生成报价"
  default_prompt: "Use $quanlaidian-quotation-skill to generate 全来店报价. Prefer the native OpenClaw form when it is available. If no form is shown, immediately switch to a low-typing guided chat flow: ask for brand, meal type, and store count; default package to 旗舰版 by meal type; present meal-type-filtered add-on and headquarter options; accept numeric, Chinese-name, or mixed replies for selections; ask center counts only when selected; route 31+ stores to human. Then generate files, show a readable quote preview, and provide downloadable links for PDF, Excel, and JSON."
